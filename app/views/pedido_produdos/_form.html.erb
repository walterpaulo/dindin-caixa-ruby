<%= form_for(@pedido_produdo, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @pedido_produdo.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4>Configura os campos abaixo:</h4>

      <ul>
      <% @pedido_produdo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :pedido, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :pedido, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :produto, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :produto, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :valor, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :valor, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Salvar", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
